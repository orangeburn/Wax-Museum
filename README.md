# 蜡像馆 单人 MVP

单人版《蜡像馆》MVP，包含 React 客户端、Express 服务端和共享类型包。当前版本固定场景为“失事潜艇”，支持创建角色、行动判定、状态推进和本地 JSON 存档恢复。

## 运行

```bash
npm install
npm run dev
```

- 客户端默认运行在 `http://localhost:5173`
- 服务端默认运行在 `http://localhost:8787`

## 测试

```bash
npm test
```

## 构建

```bash
npm run build
```
